/**
 * Viewport Configuration
 * Configuración de viewport para PWA
 */

export const viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
  themeColor: '#1e40af',
};
