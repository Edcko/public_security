/**
 * Biometric Verification (Fingerprint) API Routes
 */

import {
  POST_FINGERPRINT as POST,
} from '@/modules/integrations/biometrics/biometric.controller';

// Verificar identidad con huella digital
export { POST };
