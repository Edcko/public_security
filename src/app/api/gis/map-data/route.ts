/**
 * GIS Map Data API Routes
 *
 * Obtiene incidentes reales para mostrar en el mapa
 */

import {
  GET_MAP_DATA as GET,
} from '@/modules/gis/controllers/mapping.controller';

// Obtener datos del mapa con incidentes reales
export { GET };
